Program Modus
Kamus 
    x, j, bilangan, nX, nZero : integer
Algoritma
    input (x)
    for j = 1 to 9 do 
    input (bilangan)
    if bilangan = x then 
        nX = nX + 1
    else 
        nZero = nZero + 1
    end if
    end for
    if nX > nZero then 
        output ("Modus = ", nX)
    else 
        output ("Modus = ", nZero)
    end if
end program